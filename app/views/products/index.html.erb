<h1>Listing products</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>

  <% @products.each do |products| %>
    <tr>
      <td><%= products.title %></td>
      <td><%= products.text %></td>
    </tr>
  <% end %>
</table>